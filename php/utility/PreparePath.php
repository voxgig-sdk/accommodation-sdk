<?php
declare(strict_types=1);

// Accommodation SDK utility: prepare_path

class AccommodationPreparePath
{
    public static function call(AccommodationContext $ctx): string
    {
        $point = $ctx->point;
        $parts = [];
        if ($point) {
            $p = \Voxgig\Struct\Struct::getprop($point, 'parts');
            if (is_array($p)) {
                $parts = $p;
            }
        }
        return \Voxgig\Struct\Struct::join($parts, '/', true);
    }
}
